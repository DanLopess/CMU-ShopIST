<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/scan_product"
        android:icon="@drawable/barcode_scan"
        android:onClick="scanProduct"
        android:title="@string/scan_product"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/add_products"
        android:icon="@drawable/ic_add"
        android:onClick="addProducts"
        android:title="@string/add_product"
        app:showAsAction="ifRoom"/>

    <!--
    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:icon="@drawable/ic_settings"
        app:showAsAction="never"/>
    -->

</menu>