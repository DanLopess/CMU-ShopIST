<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_addListOverflow"
        android:icon="@drawable/ic_add"
        android:title="@string/add_list"
        app:showAsAction="ifRoom" >

        <menu>
            <item android:id="@+id/action_addList"
                android:icon="@drawable/ic_create"
                android:onClick="createNewList"
                android:title="@string/create_new_list"/>

            <item android:id="@+id/action_addListCode"
                android:icon="@drawable/ic_add"
                android:onClick="addListWithCode"
                android:title="@string/insert_a_code"/>

            <item android:id="@+id/action_addListQR"
                android:icon="@drawable/baseline_qr_code_scanner_24"
                android:onClick="addListWithQR"
                android:title="@string/scan_qr_code"/>
        </menu>
    </item>

</menu>